import React from "react";
import {
  Result,
  // @ts-ignore
} from "wieldy/antd";
import {
  Widget,
  // @ts-ignore
} from "wieldy/core";

const EmptyContent = ({ message }: any) => <Result title={message} />;

export default EmptyContent;
